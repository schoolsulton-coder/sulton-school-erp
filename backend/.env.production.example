# ===== Sulton School ERP — backend production env =====
# Serverda nusxa oling:  cp .env.production.example .env  va qiymatlarni to'ldiring

NODE_ENV=production
PORT=4000

APP_URL=https://erpsultonschool.uz
FRONTEND_URL=https://erpsultonschool.uz

# PostgreSQL — parolni O'ZINGIZNIKIGA almashtiring
DATABASE_URL=postgresql://sulton:DB_PAROLNI_ALMASHTIRING@localhost:5432/sulton_school?schema=public

# JWT — har birini `openssl rand -hex 32` bilan yarating
JWT_ACCESS_SECRET=UZUN_TASODIFIY_SATR_1
JWT_ACCESS_TTL=15m
JWT_REFRESH_SECRET=UZUN_TASODIFIY_SATR_2
JWT_REFRESH_TTL=7d

# Telegram bot (long-polling — webhook shart emas).
# DIQQAT: bir token bilan bir vaqtda faqat BITTA server ishlashi kerak (aks holda 409).
# Prod uchun alohida bot ochish tavsiya etiladi.
TELEGRAM_BOT_TOKEN=

# Sayt (Supabase) — ro'yxatdan o'tganlarni Qabulga import (deploy secret'lardan yozadi)
SUPABASE_URL=
SUPABASE_SECRET_KEY=
# SUPABASE_APPLICATIONS_TABLE=applications   # (ixtiyoriy — default 'applications')

# Birinchi superadmin (seed yaratadi)
ADMIN_PHONE=+998990000000
ADMIN_PASSWORD=KUCHLI_PAROL_ALMASHTIRING

# ===== Instagram Direct -> CRM lead (Meta webhook) =====
# META_VERIFY_TOKEN — o'zingiz o'ylab topgan maxfiy satr (Meta webhook sozlamasida ham xuddi shu)
META_VERIFY_TOKEN=
# META_APP_SECRET — Meta App -> Settings -> Basic -> App Secret (Facebook Login turi)
META_APP_SECRET=
# IG_APP_SECRET — Instagram API setup -> "Секрет приложения Instagram" (Instagram Login turi)
# Ikkalasi ham berilsa, webhook imzosi qaysi biri bilan kelsa ham qabul qilinadi.
IG_APP_SECRET=
# IG_PAGE_TOKEN — (ixtiyoriy) profil ism/username olish uchun Page access token
IG_PAGE_TOKEN=
