# ===== Sulton School ERP — frontend production env =====
# Serverda:  cp .env.production.example .env.production
# /api — nginx orqali backendga yo'naltiriladi (bir domen, CORS shart emas)
NEXT_PUBLIC_API_URL=/api
